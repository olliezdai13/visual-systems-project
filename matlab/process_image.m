function processedImg = process_image(img)
%PROCESS_IMAGE Placeholder for image processing steps.
%   Extend this function with plate detection, OCR, etc.

    % For now, simply return the original image unchanged.
    processedImg = img;
end
